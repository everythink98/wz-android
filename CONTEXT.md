# 论坛正文边界词汇

这份词汇表只描述产品语义，不绑定 React Native、Compose 或某个论坛站点。正文编译、模型、渲染和测试统一使用这些词，不再用 `row`、`part="only"` 或列表项数量猜测边界。

## 正文文档

一篇主楼、一条回复、一份签名、一条展开引用或一份 accepted answer 的逻辑正文。不同正文文档之间不连续选择。

## 语义 Owner

代码块、表格、富文本等具有稳定身份的内容单元。它的身份不随渲染预算、列表回收或展示组件改变。

## 渲染分段

语义 Owner 为满足安全或性能预算产生的有序片段。`semanticContinuation` 只说明片段是否延续同一个语义 Owner；它不表示物化区域、列表项或选择边界。

## 物化区域

一次真实挂载和回收的调度单元。正文模型只能把物化区域映射为列表项，不能把渲染分段直接映射为列表项。

## 连续选择面

一个 `selectable` 物化区域内唯一的 Android 原生选择 Owner。兼容的普通文本、标题、表格和媒体槽共享这个选择面。

## 媒体槽

连续选择面中的不可复制媒体位置。图片本身不进入剪贴板，但选择可以从媒体前继续到媒体后；媒体仍由既有媒体组件负责加载、授权、缓存和预览。

## 交互岛

具有独立手势、状态或复制行为的区域，同时也是选择边界。代码、投票、视频、折叠标题、Terminal Tab 和引用摘要属于交互岛。

## 选择边界

只由正文文档边界、交互岛或真实物化预算产生。语义 Owner 数量、渲染分段数量和 `semanticContinuation` 均不能单独产生选择边界。
