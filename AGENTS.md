# AGENTS.md

## 沟通方式

- 用户使用中文时，全程使用中文。
- 回复只说明结论、实际改动、原因和验证结果。
- 不使用工程汇报腔，不描述推进过程，不写表演化表达。
- 专有名词保持英文。
- 有多个可选方案时，列出每个方案的优缺点，并明确推荐项。

## 开始前先想清楚

- 如果需求有歧义，先指出歧义，再询问。
- 如果存在多种理解，列出不同理解和影响，不要擅自选择。
- 如果用户要求的做法明显会变复杂、变脆弱或偏离目标，要直接说明，并给出更简单的方案。
- 如果上下文不足以安全修改，停止并说明缺什么信息。

## 简洁优先

- 用能解决问题的最少改动完成任务。
- 简洁优先不等于降低目标；不能为了让 bug 消失而削弱用户要求的能力、改变既定产品方案、牺牲已有正确体验，或把用户明确想要的实现方式换成更容易做的方式。
- 不添加用户没有要求的功能。
- 不为只用一次的代码创建抽象。
- 不添加没有被要求的灵活性、配置项或扩展点。
- 不为不可能发生的场景写复杂处理。
- 如果实现明显可以更短、更直接，应改成更简单的版本。
- 判断标准：资深维护者看到这次改动，不应觉得它比问题本身更复杂。
- 修 bug 或改功能后，必须把中间错误思路留下的代码一起删掉，不保留“以后可能用”的残留。
- 新增 helper、导出、状态和测试变量，必须有真实调用或明确的边界用途；没有用途就删掉。

## 项目状态

- 当前项目是独立 Android App。
- 可用命令：`npm install`、`npm test`、`npm run typecheck`、`npm run android`、`npm run release:android`。
- 正式发布需要本机 `.env.release.local` 提供 `WZ_ANDROID_KEYSTORE_PATH`、`WZ_ANDROID_KEYSTORE_PASSWORD`、`WZ_ANDROID_KEY_ALIAS`、`WZ_ANDROID_KEY_PASSWORD`；不得提交 `.env.release.local`、keystore 或明文密码。

## 项目文档与记忆

- 需要项目背景时，先读 `README.md`。
- 接手或确认当前边界时，读 `docs/handoff.md`。
- 涉及结构、目录职责或数据边界时，读 `docs/architecture.md`。
- 涉及验证、维护或发布时，读 `docs/operator-runbook.md`。
- 需要已确认项目事实时，先读 `memory/MEMORY.md`，再按索引读取相关记忆文件。
- 当前项目事实主要在 `memory/project.md`。
- `docs/` 和 `memory/` 只作为上下文、索引和补充，不替代当前代码、用户最新要求和 `AGENTS.md`。
- 如果文档或记忆与当前代码、用户要求冲突，以用户最新要求和当前代码为准，并在回复中说明不一致处。
- 不一次性读取无关文档或记忆文件，只读取与当前任务相关的内容。

## 工作规则

- 修改前先理解现有结构，只改与任务直接相关的内容。
- 遇到 bug 涉及第三方库、框架、平台能力、渲染、网络、存储、并发、缓存、虚拟列表、手势、导航、媒体播放、WebView 或其他自己无法完全确定的外部行为时，必须查官方文档、GitHub issues/discussions 和成熟项目用法；第一次修复失败后也必须执行这一步。查完前不得回退实现、替换方案、降低能力或继续叠加猜测式规则。
- 查资料不是形式动作；必须围绕具体库名、组件名、错误现象和当前实现方式检索，并把可用结论落实到当前方案内。没有找到依据时，要说明查过什么、没找到什么，以及下一步需要什么证据。
- 不顺手重构无关代码。
- 不顺手美化相邻格式、注释或命名。
- 保持现有代码风格，即使你个人更偏好另一种写法。
- 发现无关的旧问题、死代码或可优化点时，只在回复中说明，不擅自删除或修改。
- 因本次改动产生的无用导入、变量、函数或文件，应同步清理。
- 工作区已有的用户改动必须保留，不得回滚或覆盖。
- 判断标准：每一行改动都能直接对应用户请求。
- 不把 Cookie、截图、UI dump、log、临时 bundle、生成目录或 release 产物提交。
- `android/` 是生成目录；原生配置应通过 `app.json` 和 `plugins/` 持久化。
- 在模拟器验证最新代码时，不得用 `adb uninstall`、`adb shell pm clear`、清除应用数据或重置模拟器来“确保最新”，因为这会清掉本地登录态。应优先使用覆盖安装、重启 Metro 并清 cache、`adb shell am force-stop com.wz.reader` 后重新启动 App；确实需要清数据时，必须先得到用户明确同意。
- 用户给出 V2EX、linux.do、NodeSeek、妖火的主题链接，并要求查看该详情页效果或排查原因时，应先把链接识别为对应站点和主题 id，再用模拟器 App 内详情页验证；不得用 Chrome 或桌面浏览器代替 App 内登录态验证。除非用户明确要求 App 支持系统/浏览器链接直达，否则不要把这个内部验证流程做成产品功能。
- 新增或修改依赖原站登录态的功能时，必须做“App 内登录态原站对照验证”：从 App 的 `更多 -> 账号与验证` 打开对应原站页面，确认页面仍在 `com.wz.reader` 内，并用当前登录态查看同类真实页面。原站登录态页面是权限、按钮、菜单、富文本、上传、编辑、删除、点赞等行为的事实依据；不得只靠猜接口、第三方客户端、桌面浏览器、未登录页面或作者名推断。必要时可用 WebView 调试口查看 DOM / 全局数据 / 已加载 JS / network，但不得输出或提交 Cookie、token、截图、UI dump、临时数据库等敏感或临时内容。若原站没有入口或已加载 JS/API 中没有对应行为，App 不应显示该功能；“全面测试”默认不包含真实发帖、回复、编辑、删除、点赞、投票或收藏切换。用户明确同意真实写操作验收时，临时内容必须中文且贴合主题，破坏性操作必须只作用于本次新建的临时内容，并在刷新后确认结果。
- 涉及登录、验证、详情返回、请求归属或来源解析时，必须运行相关测试和 `npm run typecheck`。
- 做清理分支时，每个阶段都必须先测试、再做模拟器真实验证、再提交，提交后才开始下一个阶段。
- 查找文件和文本优先使用 `rg`。
- 编辑前先理解已有结构和风格。
- 手工改文件时保持改动范围小。
- 不执行破坏性 git 操作，除非用户明确要求。
- 不提交、合并或发布，除非用户明确要求。
- 不把临时调试内容、无关格式化或个人偏好带进最终改动。

## 目标驱动执行

- 把任务转成可以验证的目标，而不是只按字面动作执行。
- 修复 bug 前先确认用户真正要保留的能力和验收标准；这些能力是修复边界，不能在未经用户同意的情况下被降级、删除、替换或绕开。
- 修复 bug 时必须优先尽最大努力找到根因，在当前目标和既定方案内解决；一次尝试失败不代表可以放弃，至少要完成复现、定位到共享路径、查阅相关资料、验证一个可证伪假设，再判断是否卡住。
- 如果多轮排查后仍无法在既定目标内修复，停止继续改代码，向用户报告：已经验证的事实、尝试过的方案、失败原因、剩余可选方案和各自代价；等待用户决定是否接受降级、回退或换方案。
- 只有用户明确同意，才可以把需求改成更低目标；不能把“实现困难”当作自动修改产品目标的理由。
- 添加校验时，覆盖无效输入，并确认校验生效。
- 修复 bug 时，先确认 bug 如何出现，再修到不再出现。
- 重构某处时，确认重构前后行为一致。
- 多步骤任务开始前应定义具体完成标准，不能只写“能用”“正常”“处理好”。

## 验证标准

- 每次测试先按 `docs/testing-standard.md` 选择对应功能标准；需要模拟器时对照 `docs/emulator-baseline.md`；只打开 App 不算完整测试。
- 常规改动至少运行相关测试和 `npm run typecheck`。
- 发布相关改动还要运行 `npm run release:android`。
- 只有验证通过，或遇到真实阻碍时，才回报结果。
- 有测试时运行与改动相关的测试。
- 没有测试时，用最小可行方式检查实际行为。
- 验证失败就继续修，不把未完成内容交回给用户。
- 如果因为缺少依赖、权限、数据或外部服务无法验证，必须明确说明卡点和未验证范围。

## 权衡说明

- 这些规则的目的不是拖慢简单任务，而是减少非琐碎任务中的错误假设、过度实现和无关改动。
- 对明显、安全、范围极小的任务，可以简化流程。
- 只要任务有歧义、影响范围不明或需要修改既有逻辑，就必须按上述规则执行。
